#commands_include_dir = include_directories('.')

commands_lib = static_library(
  'serialcon-commands',
  sources: ['commands.c'],
  include_directories: [
    serialcon_include_dir,
  ],
  c_args: cargs,
)

